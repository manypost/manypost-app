# refine-composer-authoring

The composer keeps the caret, states per-network capacity on the surface, requires the global text only when a channel still inherits it, and stops offering placeholders nothing substitutes
