# improve-settings-ux

Per-channel settings render with controls that fit each field (date, media, tags, named lists), and a settings field can reference org media by id, resolved to a URL at publish
